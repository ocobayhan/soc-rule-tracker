-r requirements.txt
pytest>=8.0.0
pytest-cov>=5.0.0
playwright>=1.45.0
pytest-playwright>=0.5.0
# playwright kurulumdan sonra tek seferlik tarayıcı ikili dosyalarını da
# indirmeyi ister: `python -m playwright install chromium`
